PORT=3001
RAWG_API_KEY=tu_api_key_de_https_rawg.io_apidocs
# Opcional: orígenes permitidos para CORS (separados por coma). En producción, usa la URL de tu frontend (ej. https://tu-app.vercel.app).
# CORS_ORIGIN=https://tu-app.vercel.app
# Persistencia en Render: crea un Disk (Mount Path: /data) y define:
# DB_PATH=/data/juegos.db
# Así usuarios, juegos, pendientes e insignias no se borran en deploys.
# DB_PATH=./juegos.db
# Notificaciones push (generar con: npx web-push generate-vapid-keys)
# VAPID_PUBLIC_KEY=
# VAPID_PRIVATE_KEY=

# Steam (login + logros). Obtener API key en https://steamcommunity.com/dev/apikey
# STEAM_API_KEY=tu_steam_web_api_key
# En local, Steam OpenID acepta localhost. BACKEND_URL debe ser la URL a la que Steam redirige (tu backend).
# En producción, URLs absolutas para el callback de Steam:
# BACKEND_URL=https://tu-backend.onrender.com
# FRONTEND_URL=https://tu-app.vercel.app
# En desarrollo (frontend en 5173 o 5174): BACKEND_URL=http://localhost:3001 y opcional FRONTEND_URL=http://localhost:5174
